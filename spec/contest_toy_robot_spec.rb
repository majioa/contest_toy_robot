require "spec_helper"

RSpec.describe ContestToyRobot do
  it "has a version number" do
    expect(ContestToyRobot::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
